{
  "useOptionTypes": true,
  "listType": "List",
  "definitions": [
    {
      "name": "Account",
      "description": "Account model in a sibling package",
      "methods": ["def label: String = accountId + \"/\" + active"],
      "template": {
        "accountId": "string",
        "active": "boolean",
        "openedOn": "date",
        "tier": { "type": "string", "enum": ["free", "pro"], "as": "enumType", "name": "Tier" }
      }
    }
  ]
}
